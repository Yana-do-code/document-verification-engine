version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - DEBUG=false
      - UPLOAD_DIR=uploads
    restart: unless-stopped
