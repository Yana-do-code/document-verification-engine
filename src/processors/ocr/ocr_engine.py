def run_ocr(file_path: str) -> str:
    """Extract raw text from an image or PDF using Tesseract."""
    # Placeholder: pytesseract.image_to_string(Image.open(file_path), lang="eng")
    return "Extracted text placeholder from: " + file_path
